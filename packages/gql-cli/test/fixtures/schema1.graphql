type Hello {
  world: String
}

type Query {
  greetings: String
}

type Mutation {
  changeGreetings(input: String): String
}

enum Lang {
  EN
  ES
}

interface Test {
  neat: String
}

interface TestInput {
  bar: String
}
